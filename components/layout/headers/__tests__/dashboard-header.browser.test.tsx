import { describe, expect, it } from 'vitest';
import { render } from 'vitest-browser-react';
import { DashboardHeader } from '../dashboard-header';

// describe('DashboardHeader', ()=> {
//     it('should render the DashboardHeader component', async () => {
//         const { getByTestId } = await render(<DashboardHeader title="Test Title" subtitle="Test Subtitle" />);
//         await expect.element(getByTestId('dashboard-header-mobile-layout')).toBeInTheDocument();
//         await expect.element(getByTestId('dashboard-header-desktop-layout')).toBeInTheDocument();
//     });
// });
